# Firebase web config is defined in `src/firebase/config.js`.
# Add optional local overrides here only if your setup needs them.

# ⚠️ IMPORTANT: Gemini API Key is now handled securely by the backend!
# Do NOT add the API key here. The backend Cloud Functions handle all AI requests.
# See backend/functions/.env.example for backend configuration.

