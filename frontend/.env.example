# Firebase web config is defined in `src/firebase/config.js`.
# Add optional local overrides here only if your setup needs them.
# Use the exact default bucket from Firebase Console -> Storage if uploads fail.
# Example values:
# VITE_FIREBASE_STORAGE_BUCKET=writerz-uk3312.firebasestorage.app
# VITE_FIREBASE_STORAGE_BUCKET=writerz-uk3312.appspot.com

# Backend API base URL
VITE_API_URL=https://writerz.onrender.com
